# ── Data Ingestion ──────────────────────────────────────────────────────────
yfinance>=0.2.40          # OHLCV for ETFs (20+ years, free, no key needed)
fredapi>=0.5.1            # Official FRED API (VIX, yield curve, CPI, unemployment)
pandas-datareader>=0.10.0 # Supplementary economic time series
pyarrow>=15.0.0           # Parquet storage – fast I/O, no repeated API calls
requests>=2.31.0          # HTTP utilities

# ── Data Processing ──────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.12.0

# ── Feature Engineering (Phase 2) ────────────────────────────────────────────
scikit-learn>=1.4.0

# ── ML ─ Regime Detection (Phase 3) ─────────────────────────────────────────
hmmlearn>=0.3.2
joblib>=1.3.0

# ── Portfolio Optimization (Phase 4) ─────────────────────────────────────────
PyPortfolioOpt>=1.5.5
cvxpy>=1.4.0

# ── Backtesting (Phase 5) ─────────────────────────────────────────────────────
vectorbt>=0.26.2
quantstats>=0.0.62

# ── Dashboard & API (Phase 6) ────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0
streamlit>=1.32.0
plotly>=5.20.0

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest>=8.0.0
python-dotenv>=1.0.0       # Load FRED API key from .env
seaborn>=0.13.0
matplotlib>=3.8.0
ipykernel>=6.29.0          # Jupyter notebook support
